# pipx

## What it is for

`pipx` installs Python CLI apps in isolated environments, globally usable.

## Common commands

```bash
pipx install black
pipx install httpie
pipx list
pipx upgrade-all
```

## Why use it

- Avoids polluting project environments
- Better than global `pip install` for CLI tools

## Setup

```bash
pipx ensurepath
```
